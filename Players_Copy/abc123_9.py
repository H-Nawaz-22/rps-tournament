def strategy(history):
	if not history:
		# First interaction --- history = []
		# YOUR CODE HERE --- make sure to return one of 'R', 'P', 'S'
    else:
		# Subsequent interactions -- history is nonempty
        # history is a list holding the actions of you and your opponent with the format as follows: [['R','P'],['P','S'],...]
		# YOUR CODE HERE --- make sure to return one of 'R', 'P', 'S'